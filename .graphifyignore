# Что не попадает в граф знаний.
#
# Исключаются только тяжёлые полезные нагрузки — данные и веса. Это байты,
# а не сущности с рёбрами: индексировать 21 МБ CSV и 17 МБ pickle значит
# раздуть граф и замедлить каждый запрос, ничего к нему не добавив.
#
# Документы и манифесты рядом с ними НЕ исключаются: `CONTRACT_CHANGELOG.md`,
# `README_BACKEND.md`, `handoff.json` — это как раз знание, на которое мы
# ссылаемся в CLAUDE.md. Код поставки модели (`model/ndvi/`) в граф входит:
# от него зависит наш адаптер C-02.

# Наборы данных.
data/*.csv
model/data/

# Обученные веса и предсказания.
model/runs/**/*.pkl
model/runs/**/*.csv
model/reports/**/*.csv
artifacts/**/*.joblib
artifacts/**/*.pkl
artifacts/**/*.csv
artifacts/**/*.csv.gz
artifacts/**/*.npz

# Служебное.
model/**/__pycache__/
model/notebooks/
