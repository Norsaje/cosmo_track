# NDVI: восстановление пропусков — исследовательский отчёт

Дата: 5 сентября 2026. Область работы: ML/DL, пакетный инференс и аналитика.
Веб-сервис и сбор данных по произвольному полигону в этот отчёт не входят.

---

## 1. Задача и протокол оценки

Для 2 323 контрольных строк тестового набора (`is_synthetic_gap = True`) нужно
восстановить `primary_ndvi`. Оценка: `RMSE` по этим строкам,
`GapScore = round(30 · max(0, 1 − RMSE/0.10), 2)`.

Фактические размеры данных:

| Набор | Строки | Полигоны | Видимых `primary_ndvi` | Период |
|---|---:|---:|---:|---|
| train | 99 955 | 39 | 30 520 | 2010-04-01 — 2024-10-30 |
| test | 49 190 | 20 | 13 164 | 2010-04-01 — 2024-10-30 |

Пересечение полигонов train/test пустое. Скрыто 2 323 значения — ровно 15,0 %
от измеренных значений тестовых полей. Длины серий пропусков: 2 132 одиночных,
94 двойных, 1 тройной. На контрольной строке скрыты **все** динамические поля:
три спутниковых NDVI/EVI/NDWI, погода ERA5, `year`, `doy`, `n_reference_years`.
Год и день года пересчитываются из даты.

`primary_ndvi` — это первое доступное значение в порядке
**Sentinel-2 → Landsat → MODIS**. Проверено на всех 48 161 видимых целях
с допуском 1e-7. Значит задача распадается на две: **какой спутник** снимал
в эту дату и **какое значение** он показал.

---

## 2. Разведка данных: три находки, определившие решение

### 2.1. Съёмка идёт по детерминированному календарю

| Сенсор | Правило | Покрытие |
|---|---|---|
| MODIS | все наблюдения строго при `(DOY − 1) mod 16 == 0` | 100 % |
| Landsat | до 2021 г. ровно **4 фазы** из 16 по `t mod 16` на поле | 100 % |
| Landsat | с 2021 г. те же 4 фазы + хвост от Landsat-9 | 87 % |
| Sentinel-2 | 2 фазы из 5 по `t mod 5` после 2017 г. | ~100 % |

Пример (AOI-0002, Landsat): фазы `{0, 1, 8, 9}` — два пути WRS-2 × два аппарата.
У AOI-0007 всего `{1, 9}`. Это не статистика, а орбитальная механика: набор фаз
поля постоянен внутри эпохи. Отсюда признаки «доля наблюдений этого сенсора
у этого поля в этой фазе и эпохе» и производная календарная вероятность источника.

### 2.2. 8 групп полей делят одну ячейку ERA5

Проверены все пары полей: у 8 групп температура и осадки совпадают **побитово**
на всех совместных датах (максимальная разность 0.0).

`{0002,0003,0005} {0007,0009,0011} {0019,0020,0021,0022} {0023,0024} {0026,0029} {0037,0040,0042} {0063,0065} {0064,0066}`

Шесть тестовых полей имеют такого «близнеца» в train. Это не доказывает
географическое соседство полей — только общую ячейку переанализа (~9 км).
Используется для двух вещей: восстановления скрытой погоды контрольной строки
и как приоритетный круг доноров для общей по дате аномалии.

У 16 полей из 59 погоды нет вообще — эти поля стабильно дают худший RMSE.

### 2.3. Шум одного измерения — около 0.05, и это физический предел

Три независимых измерения одной величины:

| Оценка | n | Результат |
|---|---:|---|
| Два наблюдения Landsat с интервалом 1 день | 5 529 | RMSE разности 0.0743 → σ ≈ 0.052 |
| S2 и Landsat в одну дату на одном поле | 3 329 | RMSE разности 0.0684 → σ ≈ 0.048 |
| Ошибка модели при соседе на расстоянии ≤ 1 дня | 2 044 | RMSE 0.049 |

За сутки растительность практически не меняется, поэтому расхождение — это шум
измерения: аэрозоль, тонкая облачность, тень, BRDF, различие полос и разрешения.
Восстанавливая скрытое значение, мы обязаны угадать **конкретную реализацию
этого шума**, а не только истинное состояние поля.

Насколько шум предсказуем? Общая по дате компонента (одинаковая атмосфера над
всеми полями) объясняет корреляцию 0.34–0.48 остатков — её мы используем.
Но после её вычитания σ остаётся ≈ 0.047. Проверка на готовой модели: корреляция
её остатка с усреднённым остатком других полей той же даты равна **−0.007**,
то есть канал общей аномалии исчерпан полностью.

**Вывод: RMSE ниже ~0.045–0.048 на этих данных недостижим никакой моделью.**
Цель 0.02 потребовала бы точного предсказания атмосферного шума конкретного
снимка. Мы говорим это до, а не после результата.

---

## 3. Baseline и что в нём не так

Baseline постановки — полусумма ближайших известных значений. На нашей
валидации он даёт **RMSE 0.09048 (GapScore 2.86)**.

Три его дефекта, из которых выросли гипотезы улучшения:

1. **Он смешивает три сенсора как одну кривую.** Соседи слева и справа могут
   быть Landsat и MODIS, а скрытое значение — Sentinel-2. Систематическая
   разница между сенсорами (после калибровки MAE падает с 0.053 до 0.027)
   превращается в ошибку. → Гипотеза: калибровать всё на единую шкалу S2
   и отдельно предсказывать, какой сенсор снимал.
2. **Он не знает, что дата аномальная.** Если над регионом дымка, все поля
   в этот день просели одинаково. → Гипотеза: общая по дате поправка.
3. **Он одинаково доверяет соседу в 1 день и в 15 дней** и не отличает
   выброс от нормального наблюдения. → Гипотеза: робастное локальное
   сглаживание вместо двух точек.

Детерминированная лестница проверок (без обучения, одни и те же 4 599 строк):

| Предиктор | RMSE | GapScore |
|---|---:|---:|
| Полусумма соседей (baseline) | 0.09048 | 2.86 |
| Линейная интерполяция по времени | 0.09161 | 2.52 |
| Локальная квадратичная по `primary_ndvi` | 0.09572 | 1.28 |
| Гармонизованная кривая по трём сенсорам | 0.09359 | 1.92 |
| + робастные веса (подавление выбросов) | 0.08891 | 3.33 |
| + общая по дате аномалия соседних полей | 0.08221 | 5.34 |
| + смещение спутника по фазе орбиты | **0.08139** | **5.58** |

Каждая из трёх гипотез подтвердилась и дала измеримый вклад. Линейная
интерполяция и «одна кривая по всем сенсорам» **отвергнуты**: они хуже
полусуммы, потому что расстояние до соседа хуже коррелирует с надёжностью,
чем факт смены сенсора.

---

## 4. Схема решения

```
train + видимый test
      │
      ├─ кросс-сенсорная калибровка Landsat/MODIS → шкала Sentinel-2 (робастная, на синхронных парах, со сжатием к глобальной)
      ├─ гармонизованный ряд поля → робастная локальная квадратика  → базовая кривая
      ├─ остатки наблюдений всех полей → общая по дате аномалия (все / та же ERA5-ячейка / та же культура / окно ±2 дня)
      ├─ смещение сенсора по фазе орбиты (эмпирический Байес, k = 10)
      ├─ календарь съёмки → вероятности источника
      └─ 280 признаков
            │
            ├─ классификатор источника (LightGBM, кросс-фиттинг по полигонам) → p(S2), p(Landsat), p(MODIS)
            ├─ 6 регрессоров на всей выборке (LightGBM ×4 конфигурации, CatBoost, MLP)
            ├─ 3 спутниковых эксперта: каждый учится на ВСЕХ строках, где его сенсор наблюдался
            │     ŷ = p_S2·ŷ_S2 + p_L·ŷ_L + p_M·ŷ_M
            └─ смесь с неотрицательными весами (NNLS) по валидации
                  │
                  └─ submission.csv (2 323 строки)
```

---

## 5. Протокол валидации

Ключевое отличие от предыдущей версии решения. Раньше отложенные полигоны
целиком исключались из обучения. Но на реальном тесте у нас **есть** 13 164
известных значения ровно тех 20 полей, для которых делается прогноз — это
данные условия, а не ответы. Валидация должна воспроизводить именно эту
ситуацию, иначе она измеряет не ту задачу.

Поэтому:

1. Скрываем 15 % измеренных значений 39 train-полигонов, стратифицированно
   по (поле, год) — 4 599 строк. Полученная структура серий: 4 260 одиночных,
   165 двойных, 3 тройных — те же пропорции, что в тесте.
2. Контекст = всё остальное. Обучающие примеры порождаются **поверх** этого
   контекста: оставшиеся измеренные значения делятся на 7 блоков, каждый блок
   по очереди скрывается целиком, признаки строятся на оставшемся. 5 раундов
   с разными разбиениями → 195 425 обучающих примеров из 39 106 независимых целей.
3. Это в точности процедура финального инференса, только роль скрытого теста
   играют наши 4 599 строк.

Защита от утечки: скрытие происходит **до** построения любого признака, все
динамические каналы строки обнуляются одновременно, климатология считается без
текущего года, кросс-полигонные агрегаты не содержат самого поля (оно скрыто),
вероятности источника для обучающих строк получены кросс-фиттингом по полигонам.

Оценка смеси — честная, 5-фолдовая по полигонам: веса подбираются на 4/5 полей
и применяются к отложенной 1/5.

---

## 6. Результаты

Все числа на одних и тех же 4 599 контрольных строках.

| Модель | RMSE | GapScore |
|---|---:|---:|
| Полусумма соседей (baseline) | 0.09048 | 2.86 |
| Лучший детерминированный предиктор | 0.08139 | 5.58 |
| LightGBM, 255 листьев (`lgb_c`) | **0.05068** | 14.80 |
| LightGBM, базовая конфигурация | 0.05110 | 14.67 |
| LightGBM, huber-функция потерь | 0.05104 | 14.69 |
| LightGBM, extra-trees | 0.05143 | 14.57 |
| Спутниковые эксперты, мягкая смесь | 0.05174 | 14.48 |
| CatBoost | 0.05201 | 14.40 |
| MLP 384-192-96 на остаток | 0.05745 | 12.77 |
| **Смесь (честная 5-фолдовая оценка)** | **0.05039** | **14.88** |

Точность определения источника: **98.15 %** (3 класса).

Для сравнения, присланное на вход решение: 0.05614 на своём audit
(GapScore 13.16). Снижение RMSE на 10,2 %.

Разброс: бутстрэп по полигонам даёт для абсолютного RMSE 95 %-интервал
[0.0457, 0.0561] — на 20 тестовых полях итоговое число будет плавать в этих
пределах чисто от состава выборки. Парные сравнения моделей на одних строках
устойчивее: sd разности ≈ 0.00056.

### Структура ошибки

| Срез | n | RMSE |
|---|---:|---:|
| ближайший сосед ≤ 1 дня | 2 044 | 0.0491 |
| 2 дня | 1 021 | 0.0490 |
| 3–4 дня | 712 | 0.0540 |
| 5–8 дней | 657 | 0.0574 |
| 9–16 дней | 159 | 0.0619 |
| источник Sentinel-2 | 1 647 | 0.0546 |
| источник Landsat | 1 992 | 0.0510 |
| источник MODIS | 960 | 0.0482 |

MAE 0.0313 при RMSE 0.0509: **2,2 % строк дают 47 % суммарной квадратичной
ошибки**. Это не промахи модели по форме кривой — это отдельные зашумлённые
снимки. У 100 худших строк средняя |ошибка| 0.225, знак сбалансирован (46 %
завышений), а оптимальный по RMSE прогноз для такой строки — как раз условное
среднее, а не выброс. Именно поэтому обрезка, сдвиг и калибровка прогноза
ничего не дают (проверено: клип в [−1,1] и в [0,1] — 0 изменений,
линейная калибровка даёт наклон 0.996 и выигрыш 0.00001).

---

## 7. Проверенные гипотезы: что дало прирост и что нет

| Наблюдение | Гипотеза | Проверка | Результат | Решение |
|---|---|---|---|---|
| Цель составлена из трёх сенсоров | Калибровка на шкалу S2 + отдельный прогноз источника | Те же 4 599 строк | 0.09048 → 0.08891 (детерм.), эксперты 0.05174 | принять |
| Атмосфера общая для региона | Общая по дате аномалия соседних полей | Те же строки | 0.08891 → 0.08221 | принять |
| Landsat 7/8/9 и пути дают разное NDVI | Смещение по фазе орбиты, сжатие по эмпирическому Байесу | Те же строки | 0.08221 → 0.08139; `lgb_c` 0.05101 → 0.05068 | принять |
| Видимые значения тестовых полей — данные условия | Включить их в обучение (трансдуктивно) | Смена протокола валидации | самый крупный вклад в отрыв от 0.056 | принять |
| Больше вариантов маскирования = больше выборка | 2 раунда → 5 раундов | Те же строки | 0.05206 → 0.05138 | принять |
| Остаток модели содержит ещё дату | Стекинг второго уровня на остатках по дате | corr(остаток, средний остаток других полей той же даты) | **−0.007** | **отвергнуть** — канал исчерпан |
| Соседние даты аномальны вместе | Остаток предыдущего/следующего наблюдения поля | corr с остатком базы | −0.001 / −0.059 | признаки оставлены, вклад нулевой |
| Идеальный классификатор источника сильно поможет | Оракул истинного источника | Те же строки | 0.05086 → 0.05017 | предел выигрыша 0.0007 — не приоритет |
| Прогноз смещён / нужно сжатие к кривой | Линейная калибровка, сжатие, клип | Те же строки | наклон 0.996, все варианты ≤ +0.00001 | отвергнуть |
| Есть поле-двойник с идентичным рядом | Попарный поиск по всем парам полей и сенсоров | лучший MAE 0.0277 (AOI-0037/0041) | двойников нет, донор не заменяет измерение |
| Остаточная цель проще прямой | LightGBM на `y − базовая кривая` | Те же строки | 0.05555 против 0.05138 | отвергнуть как основную |

---

## 8. Детекция аномалий

Модуль `ndvi2/anomalies.py`, чистый CPU, без сети и БД.

1. Ряд приводится к единой шкале S2 той же калибровкой, что в модели —
   иначе смена спутника читается как аномалия.
2. Норма — только по **предыдущим** годам того же поля: медиана и MAD в окне
   ±15 дней года, минимум 3 референсных года. Робастный
   `z = (x − median) / max(1.4826·MAD, 0.03)`.
3. Пороги из постановки: `z < −1` — угнетение биомассы, `z < −2` — критическая
   аномалия. Взята робастная версия, потому что MAD не разрушается одним
   облачным выбросом, в отличие от обычного σ.
4. Событием считается **устойчивое** отклонение: ≥ 2 точки, длительность
   ≥ 8 дней, разрыв внутри события ≤ 16 дней. Одиночный выброс событием не
   является — это отсекает облака.
5. `severity = площадь отрицательного отклонения (NDVI-дни) × confidence`,
   confidence растёт с числом реально наблюдённых (не восстановленных) точек.
6. Reason codes сопоставляют период с дефицитом осадков и повышенной
   температурой относительно **распределения прошлых лет** (перцентили), а не
   с абсолютным порогом. При отсутствии погодной аномалии выдаётся явное
   «возможны уборка, смена культуры или качество съёмки».

Ограничения названы прямо: разметки причин нет, поэтому precision/recall
не измерены; `confidence` — эвристическая поддержка данными, не вероятность;
детектор ретроспективный; при нехватке истории возвращается
`нет нормы`, а не вывод «поле в норме».

---

## 9. Ограничения

1. **Пол по шуму.** Остаточная ошибка — шум отдельного снимка. Дальнейший
   рост качества требует исходных растров и масок качества, а не моделей.
2. **Нет геометрии.** Соответствие `anon_polygon_id → контур` не восстановлено,
   поэтому внешние коллекции (S2 SR Harmonized, Landsat C2 L2, MOD13Q1,
   ERA5-Land) исследованы, но **не использованы**: без контура и параметров
   исходной агрегации их значения не совпадут с целевым рядом.
3. **Валидация на 39 полях, тест на 20 других.** Бутстрэп по полигонам даёт
   95 %-интервал ±0.005 RMSE — итоговый скор будет в этих пределах.
4. **Ансамбль подобран на тех же строках**, на которых меряется. Честная
   5-фолдовая по полигонам оценка (0.05039) приведена именно поэтому;
   in-sample число (0.05018) оптимистичнее.
5. **Сырые выбросы цели не обрезаны.** В данных есть значения вне [−1, 1];
   мы не знаем, исправляет ли их организатор, а OOF-эксперимент выигрыша
   от обрезки не показал.
6. **`ndvi_zscore`, `status`, `ndvi_climatology_*` из train не используются** —
   их нет в тестовой схеме, и они производны от целевого ряда.
