# Тесты проекта cosmo_track.
# tools   — unit-тесты на чистые функции (без IO);
# smoke   — проверка CLI --help и интеграции на toy fixtures;
# fixtures — детерминированные маленькие CSV (создаются make_test_fixtures.py).
